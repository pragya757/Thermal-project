apiVersion: v1
kind: Service
metadata:
  name: thermal-service
spec:
  type: NodePort
  selector:
    app: thermal
  ports:
    - protocol: TCP
      port: 80
      targetPort: 5001
      nodePort: 30007